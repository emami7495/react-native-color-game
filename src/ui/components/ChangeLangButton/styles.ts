import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  labelText: {
    margin: 24,
    fontSize: 12,
    borderRadius: 2,
    paddingVertical: 4,
    paddingHorizontal: 8,
    backgroundColor: '#ebebeb',
  },
});
